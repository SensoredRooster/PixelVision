# Detector weights

This folder is empty in git on purpose.

Place a YOLOv8/v11 ONNX file here as `yolov8n.onnx` (see the project README).
The review console runs without it; kinematics still score aim motion.
YOLO is used only to corroborate a flag with a player box.
